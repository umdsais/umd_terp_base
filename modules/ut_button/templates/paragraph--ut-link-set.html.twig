{#
/**
 * @file
 * Theme override to display a UMD Terp Link set Paragraph.
 */
#}

{% block paragraph %}
	<div{{attributes.setAttribute('id','umd_terp_paragraph--'~paragraph.id())}}>
		{% block content %}
			{{ content.field_ut_link_set_links|field_value }}
		{% endblock %}
		{{ content|without('field_ut_link_set_links') }}
	</div>
{% endblock paragraph %}
