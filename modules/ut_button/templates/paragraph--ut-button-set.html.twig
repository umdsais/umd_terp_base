{#
/**
 * @file
 * Theme override to display a UMD Terp Button set Paragraph.
 */
#}

{% block paragraph %}
	<div{{attributes.setAttribute('id','umd_terp_paragraph--'~paragraph.id())}}>
		{% block content %}
			{{ content.field_ut_button_set_buttons|field_value }}
		{% endblock %}
		{{ content|without('field_ut_button_set_buttons') }}
	</div>
{% endblock paragraph %}
